<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/mydb</property>
        <property name="hibernate.connection.username">cano</property>
        <property name="hibernate.connection.password">PnchMns7722</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Print executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Annotated entity classes -->
        <mapping class="actors.Developer"/>

        <mapping class="Buildings.OfficeSpace"/>

        <mapping class="BuildingInformation.FloorGeneral"/>

        <mapping class="BuildingInformation.RoomDev"/>
        <mapping class="BuildingInformation.RoomFactory"/>

    </session-factory>
</hibernate-configuration>