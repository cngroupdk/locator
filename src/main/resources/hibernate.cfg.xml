<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/mydb</property>
        <property name="hibernate.connection.username">cano</property>
        <property name="hibernate.connection.password">PnchMns7722</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Print executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Annotated entity classes -->
        <mapping class="locatorRoot.actors.StaffMemberFactory"/>
        <mapping class="locatorRoot.actors.Developer"/>
        <mapping class="locatorRoot.actors.HR"/>
        <mapping class="locatorRoot.actors.Tester"/>

        <mapping class="locatorRoot.BuildingInformation.FloorFactory"/>
        <mapping class="locatorRoot.BuildingInformation.FloorGeneral"/>

        <mapping class="locatorRoot.BuildingInformation.RoomFactory"/>
        <mapping class="locatorRoot.BuildingInformation.RoomDev"/>

        <mapping class="locatorRoot.Buildings.BuildingFactory"/>
        <mapping class="locatorRoot.Buildings.Headquarters"/>
        <mapping class="locatorRoot.Buildings.OfficeSpace"/>

    </session-factory>
</hibernate-configuration>